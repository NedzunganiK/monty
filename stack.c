#include "monty.h"

/**
 * f_stack - does nothing, placeholder function for future implementation
 * @head: pointer to the head of the stack
 * @counter: line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}

